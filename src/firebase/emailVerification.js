// Email verification is now handled by backend (SMTP). Use src/services/auth.js
// Email verification moved to backend (SMTP). Use `src/services/auth.js`.
export const emailVerificationService = null;
export default emailVerificationService;